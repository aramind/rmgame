import { Box, Typography } from "@mui/material";
import React from "react";

const Title = () => {
  return (
    <Box marginBottom={1}>
      <Typography variant="h1">RM GAME</Typography>
    </Box>
  );
};

export default Title;
